//import db from './database'

/*const insertResposta = db.prepare(`
    INSERT INTO Respostas (perguntaId, conteudo)
    VALUES (?, ?);
`);

// Exemplo de inserção de uma resposta para a pergunta com id 1
insertResposta.run(
  1,
  "RAM é memória volátil usada para armazenamento temporário, enquanto ROM é memória não volátil usada para armazenamento permanente."
); */
