/*const insertPergunta = db.prepare(`
    INSERT INTO Perguntas (titulo, descricao)
    VALUES (?, ?);
`);

 INSIRA uma pergunta aqui
insertPergunta.run(
  "Qual a diferença entre RAM e ROM?",
  "Estou confuso sobre a diferença entre memória RAM e ROM..."
); */
